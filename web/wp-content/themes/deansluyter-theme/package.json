{
  "name": "deansluyter-theme",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "mkdir -p dist/.vite && touch dist/.vite/hot && vite",
    "build": "rm -f dist/.vite/hot && vite build"
  },
  "devDependencies": {
    "@fortawesome/fontawesome-free": "^5.15.4",
    "breakpoint-sass": "^2.7.1",
    "sass": "^1.77.8",
    "vite": "^5.4.10"
  }
}
